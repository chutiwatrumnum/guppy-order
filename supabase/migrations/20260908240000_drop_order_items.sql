-- ทิ้งตาราง order_items
--
-- มาจาก schema ตั้งต้นตอน dump ครั้งแรก (20260322131057_remote_schema)
-- แต่แอปเก็บรายการปลาเป็น jsonb ในคอลัมน์ orders.items แทนมาตลอด
--
-- ตรวจแล้ว: 0 แถว และไม่มีโค้ดที่ไหนอ้างถึงเลย ทั้งแอปและบอท
-- (get_public_order อ่าน o.items ซึ่งคนละอันกัน)
--
-- ⚠️ ลบแล้วกู้ไม่ได้ — แต่ไม่มีอะไรให้กู้ ตารางว่างเปล่า
drop table if exists public.order_items;
