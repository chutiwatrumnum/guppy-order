# 🤖 สิ่งที่ต้องแก้ในบอท LINE (`line-tacking`)

> ตรวจโค้ดบอทจริงแล้ว — ของที่คิดว่าต้องทำ **ทำไปแล้วเกือบหมด**
> ไฟล์นี้เหลือเฉพาะที่ยังไม่ได้ทำจริง ๆ

## ✅ ที่ทำไปแล้ว ไม่ต้องแตะ

| | ที่ไหน |
|---|---|
| ยุบข้อความ + การ์ด ให้อยู่ใน push call เดียว | `index.js:214`, `index.js:297` |
| กรอง push ด้วย `statusTier()` (ยุบ hop ย่อยใน 1xx) | `index.js:291-294`, `333` |
| เก็บ `last_status` ทุกครั้งแม้ตอนไม่ push | `index.js:289` |
| "รายการ" ตอบเป็นการ์ดพร้อมสถานะ ผ่าน **reply (ฟรี)** | `index.js:381-411` |
| ไปรษณีย์ล่มก็ยังตอบรายการเลขได้ ไม่เงียบใส่ลูกค้า | `index.js:368-378` |
| เช็กสถานะ batch ทุกเลขใน API call เดียว | `index.js:277` |
| แจ้งเฉพาะ 8:00–23:00 เวลาไทย | `index.js:248-260` |
| cron **ไม่** ส่ง `parcel_delivered` — แค่ unsubscribe | `index.js:320` |

---

## 🔴 ที่ยังไม่ได้ทำ — เผา reply token ทิ้งกับข้อความ "กำลังตรวจสอบ"

**`index.js:180`** ตอบ `🔍 กำลังตรวจสอบพัสดุ ... ` ด้วย `replyMessage`
แล้วผลจริงตามไปทีหลังด้วย `pushMessage` ที่ **`index.js:214`**

```
reply (ฟรี)  →  "🔍 กำลังตรวจสอบพัสดุ ..."   ← ไม่มีข้อมูลอะไรเลย
push (เสียโควต้า) → การ์ด + สถานะจริง        ← อันที่ลูกค้าอยากได้
```

**ใช้โควตาฟรีไปกับข้อความที่ไม่มีเนื้อหา แล้วจ่ายเงินให้ข้อความที่มีเนื้อหา — สลับกันพอดี**

ทุกครั้งที่ลูกค้าส่งเลขพัสดุมาเช็ก = เสีย 1 โควต้า ทั้งที่ควรฟรี
และยิ่ง commit ล่าสุด (`Put the movement history one tap from a carousel card`)
ทำให้กดดูง่ายขึ้น ลูกค้าก็ยิ่งเช็กบ่อยขึ้น ค่าใช้จ่ายตรงนี้จะโตตามการใช้งาน

### แก้ยังไง

ถือ reply token ไว้ เรียก `trackParcel()` ให้เสร็จก่อน แล้วค่อย `replyMessage`
ผลจริงกลับไปทีเดียว — ตัดข้อความ "กำลังตรวจสอบ" ทิ้ง

`trackParcels` แบบ batch ใน `myParcels` ก็ทำแบบนี้อยู่แล้ว (`index.js:367`)
คือรอผลก่อนแล้วค่อย reply — เอาแพตเทิร์นเดียวกันมาใช้

> ⚠️ **ต้องมี timeout** — reply token มีอายุจำกัด ถ้าไปรษณีย์ตอบช้าจนหมดอายุ
> จะ reply ไม่ได้เลย ให้ตั้ง timeout ~3 วิ แล้วถ้าไม่ทันค่อยตกไปเป็น push
> (แบบนั้นแย่สุดคือเท่าเดิม ไม่ใช่แย่กว่าเดิม)

---

## 🟡 อ่านค่าจากหน้าตั้งค่า (ถ้าอยากปรับได้โดยไม่ deploy)

migration `20260908120000_parcel_pull_tracking.sql` เพิ่มไว้ให้แล้ว 2 ปุ่ม

### 1. `settings.parcel_push_tiers` (integer[], default `{1,3,4,5}`)

ตอนนี้ `index.js:296` push ทุกครั้งที่ `currentTier > lastTier`
เพิ่มเงื่อนไขว่า `currentTier` ต้องอยู่ในชุดนี้ด้วย — **ยัง `update_status` เสมอ กรองแค่การ push**

```js
const tiers = settings.parcel_push_tiers || [1, 3, 4, 5];
if (currentTier > lastTier && tiers.includes(currentTier)) {
  // ...push
}
```

ต้องเก็บ `currentTier > lastTier` ไว้ด้วย ไม่งั้น hop ย่อยในขั้นเดียวกัน
(201 → 206 → 211 ล้วนเป็นขั้น 2) จะยิงซ้ำทุกครั้ง

| ชุด | ขั้นที่แจ้ง | /พัสดุ |
|---|---|---|
| `{1,2,3,5}` | เท่าพฤติกรรมเดิม | ~4 |
| **`{1,3,4,5}`** | รับเข้าระบบ · นำจ่าย · ตีกลับ · สำเร็จ | **~3** |
| `{3,4,5}` | นำจ่าย · ตีกลับ · สำเร็จ | ~2 |
| `{3,4}` | นำจ่าย · ตีกลับ | ~1–2 |
| `{}` | ไม่ push เลย | 0 |

> อย่าตัดขั้น **4 (นำจ่ายไม่สำเร็จ)** ทิ้ง — แปลว่าปลาค้างอยู่ที่ไปรษณีย์
> เป็นข้อความที่มีค่าที่สุดในชุดสำหรับของเป็น ๆ และนาน ๆ ทีถึงจะยิงที
> จึงแทบไม่กินโควต้าอยู่แล้ว

### 2. `message_templates.enabled` (boolean)

`templates.js:26` ดึงแค่ `key, body` — เพิ่ม `enabled` เข้าไปแล้วข้ามแถวที่ปิดอยู่

```js
const { data, error } = await supabase
  .from('message_templates')
  .select('key, body, enabled');
// ...
cache = Object.fromEntries((data || []).filter((r) => r.enabled).map((r) => [r.key, r.body]));
```

⚠️ แต่ `render()` มี fallback ฝังในโค้ด (`templates[key] || fallback`)
**ปิดแล้วมันจะไปใช้ fallback ต่อ = สวิตช์ไม่มีผล** ต้องให้ `render()` คืนค่าว่าง
เมื่อแถวมีอยู่แต่ถูกปิด แล้วฝั่งเรียกข้ามการส่งไป — ไม่ใช่แค่กรองตอน `load()`

---

## เช็กลิสต์

- [ ] ตัด "🔍 กำลังตรวจสอบ" แล้ว reply ผลจริงแทน (+ timeout 3 วิ)
- [ ] `index.js:296` เช็ก `settings.parcel_push_tiers.includes(currentTier)`
- [ ] `templates.js` อ่าน `enabled` และแยก "ปิดอยู่" ออกจาก "ไม่มีแถว"
